.clearer.hide-responsive  
%br.hide-responsive/
.main-content
  / full slider from here
  = render "experimental/partials/fullslider"
  / %br.clearer/
  / spotlight starts from here
  = render "experimental/partials/spotlight"
  .clearer
  %br.clearer/
.main-content-left
  / tabs starts from here
  = render "experimental/partials/tabs_short"
  / trending starts from here
  %br.clearer
  = render "experimental/partials/trending_short"
  / review directory
  %br.clearer
  = render "experimental/partials/review_directory"
  / review directory b
  = render "experimental/partials/review_directory_b"
  / review directory c
  = render "experimental/partials/review_directory_c"
  .clearer.hide-responsive
  %br.clearer/
/side bar
.sidebar
  - if current_user.nil?
    /login box
    = render "experimental/partials/login_box"
  -else
    /profile box
    = render "experimental/partials/profile_box"

  / social login box
  = render "experimental/partials/social_login_box"
  / member box
  = render "experimental/partials/member_box"
  /rating box
  = render "experimental/partials/rating_box"
  /rating box
  = render "experimental/partials/rating_box2"
  / left bar
  / = render "experimental/partials/left_bar"
  /widget top
  = render "experimental/partials/widget_top"
  / .clearer.hide-responsive
  / %br.clearer/
  / newly reviewd
  = render "experimental/partials/newly_reviewed"
  /popular box
  = render "experimental/partials/popular_box"
  /banner box
  = render "experimental/partials/banner_box"


  