.clearer.hide-responsive  
%br.hide-responsive/
.main-content-left
	.page-content.review.single-post
		%link{:href => "http://www.industrialthemes.com/made/wp-content/uploads/2012/06/misc1-160x160.jpg", :rel => "image_src"}/
		.review-content
			.ribbon-shadow-left
			.section-wrapper
				.section-header
					Tier 7
			.section-arrow
			/ post by removed from here
			.content-panel
				.the-content.justify-content
					= form_for @profile_tier7, :url => {:controller => :tier7, :action => :create},:html => {:class => "exp-contact-form"}, :method => :post do |f|
						%p{:class => "ask2"}
							= f.label "(first diagnosis) Who was the first person you spoke to after you received your initial positive test result? Why did you choose to tell this person?",:class=>"label"
						= f.text_area :first_diagnosis, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(sex & HIV) How has your HIV status affected your sex life?",:class=>"label"
						= f.text_area :sex_hiv, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(relationships & HIV) How has it affected love and relationships?",:class=>"label"
						= f.text_area :relationship_hiv, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(disclosure) How do you decide whether to tell someone your HIV status?",:class=>"label"
						= f.text_area :disclosure, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(relationships & HIV) Has your relationship with your family and friends changed or evolved after you found out you are HIV-positive? If so, how?",:class=>"label"
						= f.text_area :family_hiv, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(work & HIV) How has living with HIV affected your current work?",:class=>"label"
						= f.text_area :work_hiv, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(discrimination) Have you ever experienced discrimination on the basis of your HIV status from government agencies (eg. police, health workers, schools, employment officials, immigration agencies) or other areas (employers, businesses, clubs, etc). If so, please tell us what happened",:class=>"label"
						= f.text_area :discrimination, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(disclosure, stigma) What needs to happen in order for people living with HIV to feel more comfortable about telling people about their status?",:class=>"label"
						= f.text_area :disclosure_stigma, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(acceptance) Stigma and discrimination against people living with HIV is often quite challenging for everyone. What is one simple thing that people could do to encourage more acceptance in the community?",:class=>"label"
						= f.text_area :acceptance, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "(treatment) How do you feel about antiretrovial treatment?",:class=>"label"
						= f.text_area :treatment, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "Can you share one humorous or odd thing about your life with HIV?",:class=>"label"
						= f.text_area :humorous_odd, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"
						%p{:class => "ask2"}
							= f.label "How do you feel about your future?", :class=>"label"
						= f.text_area :future_life, :disabled => @is_disable, :rows => 5, :class=>"textarea", :style => "width: 630px"

						%p
						- unless @is_disable
							= link_to "<i class='icon-check'></i>Submit".html_safe, "javascript:void(0)", :class => "submit-button", :id => "ask_question_link", :style => "margin-bottom: 10px;"
.sidebar
	- if current_user.nil?
		/login box
		= render "experimental/partials/login_box"
	-else
		/profile box
		= render "experimental/partials/profile_box"

	/ social login box
	= render "experimental/partials/social_login_box"
	/ member box
	/ = render "experimental/partials/member_box"
	/rating box
	= render "experimental/partials/rating_box"
	/rating box
	= render "experimental/partials/rating_box2"
	/ left bar
	/ = render "experimental/partials/left_bar"
	/widget top
	= render "experimental/partials/widget_top"
	/ .clearer.hide-responsive
	/ %br.clearer/
	/ newly reviewd
	/ = render "experimental/partials/newly_reviewed"
	/popular box
	= render "experimental/partials/popular_box"
	/banner box
	= render "experimental/partials/banner_box"

:javascript
	jQuery("#ask_question_link").click(function (){
		jQuery("#new_profile_tier7,#edit_profile_tier7").submit();
	});