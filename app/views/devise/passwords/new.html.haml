= image_tag "plus/body_about.jpg", :class => "body_header"
.alpha.grid_24.body_about{:id => "firstElement", :style => "height: 622px !important;"}
  %span{:class => "p_a"}
    Forgot password
  %span{:class => "p_a1",:style=>"position: relative !important; left: -309px;"}
    = link_to "Home", "/"
    > Forgot password
  %h2
    = t('devise.passwords.link')
  .ask
    %p.ask1
      = t('devise.passwords.send_instructions')
    = form_for resource, :as => resource_name, :url => password_path(resource_name) do |f|
      = f.error_messages
      %p.ask2
        %strong
          = f.label :email
      .search3
        = f.text_field :email
      .ask1
        .buttons
          = f.submit t('devise.passwords.send_button'), :class => "save"
        = render :partial => "shared/devise_links"
