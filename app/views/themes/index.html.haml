.row-fluid
  .span12
    .box
      .title
        %h4
          %span{:class => "icon16  entypo-icon-palette"}
          %span
            Themes
        %form{:class => "box-form right", :action => "", :style => "margin-top:-27px !important"}
          <a class="btn dropdown-toggle" data-toggle="dropdown" href="#" style="padding: 4px 10px !important; border-width: 1px !important; border-style: solid !important">
          <span class="icon16 iconic-icon-cog"></span>
          <span class="caret"></span>
          </a>
          %ul{:class => "dropdown-menu"}
            %li
              = link_to "<span class='icon16 brocco-icon-plus'></span>Add new theme".html_safe, "#", :id => "openModalDialog"
        = link_to "Minimize", "#", :class => "minimize"
      .content{:style => "Padding-bottom: 50px !important"}
        %table{:cellpadding=>0,:cellspacing=>0,:border=>0,:class=>"responsive dynamicTable display table table-bordered",:width=>"100%"}
          %thead
            %tr
              %th
                Name
              %th 
                Description
              %th
                Color
              %th
                Date
              %th 
                Action
          %tbody
            -@themes.each do |theme|
              = render :partial => "theme", :object => theme
        / commented can be use in future
        /= t('themes.index.description')

        / .toolbar
        /   %ul.menubar
        /     %li.action
        /       = link_to "Add new theme", new_theme_path
        /     %li.action
        /       = link_to "Import", "#", class: "import_theme"
        /     %li.more.current.action
        /       = link_to "Filter", "#"
        /       %ul.drop-menu
        /         %li
        /           = link_to t('themes.index.group_themes'), themes_path
        /         %li
        /           = link_to t('themes.index.all_themes'), themes_path(:tab => "all")

        / = form_tag import_themes_path, :method => :post, :class => "import_dialog form_dialog" do |f|
        /   =file_field_tag :theme_file
        /   =submit_tag "import"

        / %section.themes
        /   = render "theme", :theme => current_group.current_theme || Theme.where(:is_default => true).first

        /   -if @themes.empty?
        /     = render "shared/empty", :type => "themes", :url => new_theme_path
        /   -else
        /     -@themes.each do |theme|
        /       = render "theme", :theme => theme

        /   = paginate @themes
