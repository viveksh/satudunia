- content_for :title do
  Step 3

Terms of use page 3
%br
Lorem ipsum dolor sit amet

- if @auth_provider == 'facebook' && current_group.has_facebook_login?
  - button_to "Next", '/users/auth/facebook'
- elsif @auth_provider == 'google'
  = button_to "Next", multiauth_url("Google")
- elsif @auth_provider == 'twitter'
  = button_to "Next", multiauth_url("Twitter")