#forums-dir-list-newest.dir-list
  #pag-top.pagination
    #member-dir-count-top.pag-count
      Viewing Questions #{@paginator.options[:skip]} to #{@paginator.options[:skip]+@paginator.options[:limit]} (of #{@paginator.count} active members)
    #members-dir-list.members.dir-list1
      = paginate @paginator